<%= form_for(current_user.relationships.build(followed_id: @user.id), remote: true) do |f| %>
  <div><%= f.hidden_field :followed_id %></div>  <!--リクエスト以外にも情報を送りたい-->
  <%= f.submit "Follow", class: "btn btn-large btn-primary" %>
<% end %>

